# binaries
/bin/
*.exe

# experiment output (raw per-run JSON can get large; summaries are committed)
/results/raw/

# local env overrides
.env
.env.local

# editor noise
.vscode/
.idea/
*.swp

# tooling scratch (node modules pulled into the mounted dir by the shot runner)
/tools/shots/node_modules/
/tools/shots/package*.json
/docs/report/report.html

# grafana/prometheus local state (mounted volumes)
/deploy/grafana/data/
/deploy/prometheus/data/
